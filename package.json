{
  "name": "signtool",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "update": "tsc && tfx build tasks upload --task-path .",
    "package": "tsc && tfx extension create --manifest-globs vss-extension.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "vso-node-api": "^6.5.0",
    "vsts-task-lib": "^2.4.0"
  },
  "devDependencies": {
    "@types/node": "^9.6.2",
    "@types/q": "^1.5.0"
  }
}
